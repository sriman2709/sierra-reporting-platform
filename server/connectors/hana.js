/**
 * hana.js — Active data connector (HANA direct)
 * To swap to Datasphere OData: change the import in this file only.
 * All modules import from this file — nothing else changes.
 */
export { query, SCHEMA } from '../config/db.js';
